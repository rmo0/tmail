{
  "name": "tmail",
  "version": "1.0.0",
  "description": "Temporary Gmail (emailnator) client: generate inbox, list messages, get body. No parsing—use your own (e.g. cheerio) to extract codes.",
  "main": "index.js",
  "scripts": {
    "test": "node -e \"require('.'); console.log('tmail load ok');\""
  },
  "keywords": [
    "temp-mail",
    "emailnator",
    "disposable-email",
    "gmail"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "https-proxy-agent": "^7.0.2",
    "node-fetch": "^2.7.0"
  }
}
